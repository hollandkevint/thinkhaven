/**
 * Pathway labels — safe to import from client components.
 * Separated from session-primitives.ts which imports server-only code.
 */

export {
  type PathwayType,
  PATHWAY_LABELS,
} from './pathway-config';
